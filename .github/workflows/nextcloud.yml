name: Provision Gravitas Nextcloud

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/nextcloud.yml'
      - 'ops/nextcloud/**'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/nextcloud.yml'
      - 'ops/nextcloud/**'
  workflow_dispatch:
  workflow_run:
    workflows: ['Deploy Gravitas to Strato']
    types: [completed]

permissions:
  contents: write

concurrency:
  group: gravitas-nextcloud
  cancel-in-progress: false

env:
  DOMAIN: gravitasplus.com

jobs:
  validate:
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout proposed Nextcloud changes
        uses: actions/checkout@v4

      - name: Validate provisioning scripts without touching production
        shell: bash
        run: |
          set -euo pipefail
          bash -n ops/nextcloud/ensure.sh
          bash -n ops/nextcloud/apps.sh
          bash -n ops/nextcloud/nginx-guard.sh
          grep -q 'groupfolders required' ops/nextcloud/apps.sh
          grep -q 'user_oidc' ops/nextcloud/apps.sh
          grep -q 'gravitas-nextcloud-nginx-guard.path' ops/nextcloud/nginx-guard.sh
          echo 'Nextcloud code validation passed; production provisioning only runs after a successful Gravitas deploy or manual dispatch.'

  nextcloud:
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Resolve SSH
        shell: bash
        run: |
          set -euo pipefail
          test -n "${{ secrets.DEPLOY_HOST }}" || { echo 'Missing DEPLOY_HOST'; exit 1; }
          test -n "${{ secrets.DEPLOY_SSH_KEY }}" || { echo 'Missing DEPLOY_SSH_KEY'; exit 1; }
          USERNAME="${{ secrets.DEPLOY_USER }}"
          PORT="${{ secrets.DEPLOY_PORT }}"
          [ -n "$USERNAME" ] || USERNAME=deploy
          [ -n "$PORT" ] || PORT=22
          echo "CONFIGURED_USER=$USERNAME" >> "$GITHUB_ENV"
          echo "DEPLOY_PORT=$PORT" >> "$GITHUB_ENV"

      - name: Prepare SSH
        shell: bash
        run: |
          set -euo pipefail
          install -d -m 700 ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/gravitas_deploy
          chmod 600 ~/.ssh/gravitas_deploy
          ssh-keygen -y -f ~/.ssh/gravitas_deploy >/dev/null
          ssh-keyscan -p "$DEPLOY_PORT" -H "${{ secrets.DEPLOY_HOST }}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
          OPTS=(-i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" -o BatchMode=yes -o ConnectTimeout=10)
          HOST="${{ secrets.DEPLOY_HOST }}"
          if ssh "${OPTS[@]}" "$CONFIGURED_USER@$HOST" true 2>/dev/null; then
            EFFECTIVE_USER="$CONFIGURED_USER"
          elif [ "$CONFIGURED_USER" != root ] && ssh "${OPTS[@]}" "root@$HOST" true 2>/dev/null; then
            EFFECTIVE_USER=root
          else
            echo 'SSH authentication failed'
            exit 1
          fi
          echo "DEPLOY_USER=$EFFECTIVE_USER" >> "$GITHUB_ENV"

      - name: Provision or repair Nextcloud
        shell: bash
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            'sudo rm -f /tmp/gravitas-nextcloud-ensure'
          scp -i ~/.ssh/gravitas_deploy -P "$DEPLOY_PORT" ops/nextcloud/ensure.sh \
            "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}:/tmp/gravitas-nextcloud-ensure"
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            "sudo install -m 750 /tmp/gravitas-nextcloud-ensure /usr/local/sbin/gravitas-nextcloud-ensure && sudo rm -f /tmp/gravitas-nextcloud-ensure && sudo /usr/local/sbin/gravitas-nextcloud-ensure '$DOMAIN' repair"

      - name: Install collaboration app suite
        shell: bash
        run: |
          set -euo pipefail
          scp -i ~/.ssh/gravitas_deploy -P "$DEPLOY_PORT" ops/nextcloud/apps.sh \
            "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}:/tmp/gravitas-nextcloud-apps"
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            'sudo install -m 750 /tmp/gravitas-nextcloud-apps /usr/local/sbin/gravitas-nextcloud-apps && sudo rm -f /tmp/gravitas-nextcloud-apps && sudo /usr/local/sbin/gravitas-nextcloud-apps'

      - name: Install persistent Nginx route guard
        shell: bash
        run: |
          set -euo pipefail
          scp -i ~/.ssh/gravitas_deploy -P "$DEPLOY_PORT" ops/nextcloud/nginx-guard.sh \
            "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}:/tmp/gravitas-nextcloud-nginx-guard"
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            "sudo install -m 750 /tmp/gravitas-nextcloud-nginx-guard /usr/local/sbin/gravitas-nextcloud-nginx-guard-install && sudo rm -f /tmp/gravitas-nextcloud-nginx-guard && sudo /usr/local/sbin/gravitas-nextcloud-nginx-guard-install '$DOMAIN'"

      - name: Verify public endpoint
        shell: bash
        run: |
          set -euo pipefail
          curl -4 -fsS --retry 20 --retry-delay 4 --connect-timeout 8 --max-time 25 \
            "https://$DOMAIN/nextcloud/status.php" | tee /tmp/status.json
          grep -q '"installed":true' /tmp/status.json
          curl -4 -fsSI --retry 8 --retry-delay 3 --connect-timeout 8 --max-time 25 \
            "https://$DOMAIN/nextcloud/" | grep -Eq '^HTTP/.* (200|302|303)'

      - name: Verify containers, apps, route guard, cron and backup timers
        shell: bash
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" 'bash -s' <<'REMOTE'
          set -euo pipefail
          sudo docker inspect -f '{{.State.Running}}' gravitas-nextcloud | grep -qx true
          sudo docker inspect -f '{{.State.Running}}' gravitas-nextcloud-db | grep -qx true
          sudo docker inspect -f '{{.State.Running}}' gravitas-nextcloud-redis | grep -qx true
          sudo systemctl is-active gravitas-nextcloud-cron.timer
          sudo systemctl is-active gravitas-nextcloud-backup.timer
          sudo systemctl is-active gravitas-nextcloud-nginx-guard.path
          sudo docker exec -u www-data gravitas-nextcloud php occ status | grep -q 'installed: true'
          sudo docker exec -u www-data gravitas-nextcloud php occ app:list --enabled | grep -Eq '^[[:space:]]*-[[:space:]]+groupfolders:'
          sudo docker exec -u www-data gravitas-nextcloud php occ config:system:get overwritewebroot | grep -qx '/nextcloud'
          sudo docker exec -u www-data gravitas-nextcloud php occ config:system:get overwriteprotocol | grep -qx 'https'
          sudo grep -q '# BEGIN GRAVITAS NEXTCLOUD' /etc/nginx/sites-available/gravitas
          REMOTE

      - name: Record verified status on status branch
        shell: bash
        run: |
          set -euo pipefail
          VERSION=$(ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            "sudo docker exec -u www-data gravitas-nextcloud php occ status" | sed -n 's/^  - versionstring: //p' | head -n1)
          [ -n "$VERSION" ] || VERSION=unknown
          printf 'status=live\nurl=https://%s/nextcloud/\nversion=%s\nteam_folders=enabled\nnginx_guard=enabled\nverified_at=%s\n' \
            "$DOMAIN" "$VERSION" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > .nextcloud-status
          git config user.name 'gravitas-nextcloud-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add .nextcloud-status
          git commit -m 'Record verified Nextcloud status'
          git push --force origin HEAD:nextcloud-status

      - name: Export one-day admin credentials
        shell: bash
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/gravitas_deploy -p "$DEPLOY_PORT" "$DEPLOY_USER@${{ secrets.DEPLOY_HOST }}" \
            'sudo cat /etc/gravitas/nextcloud-admin-credentials' > nextcloud-admin-credentials.txt

      - name: Upload one-day admin credentials
        uses: actions/upload-artifact@v4
        with:
          name: nextcloud-admin-credentials
          path: nextcloud-admin-credentials.txt
          retention-days: 1
          if-no-files-found: error
